import React, { useState } from "react";
import product from "./product";

const Cleanup = () => {
    // const [myproduct, setmyproduct] = useState(product);
    
    

    let inputChangeHandler = (ev) => {
        // console.log(ev.target.value)
        console.log("Clicked")
    }

  return (
    <div>
        <input type="text" placeholder="Enter Text" onChange={inputChangeHandler} />
        <button>Send</button>
        {/* {myproduct.map((p, indx) => <div key={indx}>{p}</div>)} */}
    </div>
  );
};

export default Cleanup;

-> 1.06.04 minutes pr pic click krna h for expaination
-> 2.10.04 minutes pr pic click krna h for expaination
-> 2.37.18 minutes pr pic click krna h for expaination


3. 3rd explaination "Search product"
import React, {useState, useEffect} from "react";
import initialProduct from "../Cleanup/product";

const SearchProduct = () => {
	const [product, setproduct] = useState(initialProduct)
    const [searchItem, setsearchItem] = useState('');
    

	useEffect(() => {
		let id = setTimeout(() => {
            let filteredItem = initialProduct.filter(p => {
                if(p.toLowerCase().includes(searchItem.toLowerCase())) {
                    return true;
                }
                return false;
            })
            setproduct(filteredItem)
			// console.log("Running Effect", filteredItem);
		}, 1000);

		return () => {
			clearTimeout(id);
		};
	}, [searchItem]);

	let inputChangeHandler = (ev) => {
		setsearchItem(ev.target.value);
	};
	return (
		<>
			<input
				type="text"
				placeholder="Enter Text"
				onChange={inputChangeHandler}
			/>
			<button>Send</button>   

            {product.map((p, indx) => {
                return <div key={indx}>{p}</div>
            })}
		</>
	);
};

export default SearchProduct;

- onChange
- onBlur